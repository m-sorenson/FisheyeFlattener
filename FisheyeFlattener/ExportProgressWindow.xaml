<Window x:Class="FisheyeFlattener.ExportProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="Assets/AppIcon.ico"
        Title="Exporting" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="ToolWindow" ShowInTaskbar="False">
    <!-- SizeToContent="Height" (rather than a guessed fixed Height) so the window
         always fits its content - a fixed height previously clipped the Cancel
         button at the bottom. -->
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Overall progress" Opacity="0.7" Margin="0,0,0,4"/>
        <ProgressBar x:Name="OverallProgressBar" Grid.Row="1" Height="16" Minimum="0" Maximum="100" Margin="0,0,0,14"/>

        <TextBlock x:Name="StageTextBlock" Grid.Row="2" Text="Exporting..." TextWrapping="Wrap" Margin="0,0,0,4"/>
        <ProgressBar x:Name="StageProgressBar" Grid.Row="3" Height="16" Minimum="0" Maximum="100" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Cancel" Padding="12,5" Click="CancelButton_Click"/>
            <Button x:Name="OpenLocationButton" Content="Open Export Location" Padding="12,5" Margin="8,0,0,0"
                    Visibility="Collapsed" Click="OpenLocationButton_Click"/>
            <Button x:Name="CloseButton" Content="Close" Padding="12,5" Margin="8,0,0,0"
                    Visibility="Collapsed" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
